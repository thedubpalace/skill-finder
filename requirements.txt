# --- Web framework ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# --- Database ---
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# --- Embeddings / semantic search ---
# Option A: local (no API key needed, runs on CPU)
sentence-transformers>=3.0.0
# Option B: swap for OpenAI embeddings by setting EMBEDDING_BACKEND=openai
# openai>=1.30.0

# --- Vector similarity (in-process, no extra server) ---
faiss-cpu>=1.8.0

# --- Crawler / HTTP ---
httpx>=0.27.0
beautifulsoup4>=4.12.0          # PyPI search-page scrape
lxml>=5.2.0
# YouTube Data API v3 — crawler calls the REST endpoint via httpx, so this is optional.
# Install if you prefer the official client: google-api-python-client>=2.130.0

# --- Scheduling / rate-limit ---
tenacity>=8.3.0
apscheduler>=3.10.0

# --- Utilities ---
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0
rich>=13.7.0
